{
    "builds": [{
        "src": "noticeboard/wsgi.py",
        "use": "@ardnt/vercel-python-wsgi",
        "config": { "maxLambdaSize": "15mb" }
    }],
    "routes": [{
        "src": "/(.*)",
        "dest": "noticeboard/wsgi.py"
    }]
}